# lock_window.ahk / launcher.ahk 仕様書

新しいチャットでの引き継ぎ用。両ファイルの最新版と一緒に渡してください。

---

## 直近の変更履歴

- **食事休憩の手動終了ボタンを実装。** 本仕様書には以前から「食事休憩中は数字表示を隠し `⏭ 食事休憩を終了` ボタンを表示する」という仕様が記載されていたが、`lock_window.ahk` には未実装だった（`OnMealEnd` 関数・`mealEndBtn` コントロールともに存在しなかった）。今回追加し、仕様と実装を一致させた。自動終了（時間帯超過）・手動終了（ボタン）の両方が `EndMealPauseResume()` という共通関数を呼ぶ形に整理。
- **クロッキーと食事休憩の干渉バグを修正。** `CheckMealPause` の冒頭で `isCroquis` なら早期リターンするようにした（詳細は下記「既知の問題・注意点」参照）。
- **重複コードの削除。** `lock_window.ahk` で `workWindowTitles` / `workWindowProcesses` が2箇所に重複定義されていた（コメント見出しごと丸ごとコピペされたもの）ため、古い方を削除。
- **launcher.ahk のデッドコード削除。** 就寝ブロックの自動スケジュール方式（`ScheduleNightBlock` と、それが使うPS1事前生成処理）は手動トリガー方式への移行後は呼ばれておらず、起動のたびに無駄なファイル書き込みが発生していたため削除。あわせて、同じく使われていなかった `night_debug.txt` デバッグログと `nightBlockHourStart/End`（時間帯判定用・未使用）設定も削除。
- **NextDNS関連変数の命名統一。** launcher.ahk 側だけ `nightdnsApiKey` 等「night」接頭辞になっており、lock_window.ahk 側の `nextdnsApiKey` 等「next」接頭辞と紛らわしかったため、launcher.ahk 側も `nextdns*` に統一。
- **スリープ解除ルーティンの手動実行機能を追加。** トレイメニューに「🔁 スリープ解除ルーティンを手動実行」を追加。自動起床時の処理（就寝ブロック解除→待機→クロッキー→作業）を `StartWakeRoutine()` として共通化し、手動メニューからも同じ関数を呼べるようにした。二重起動防止のため `wakeRoutineActive` フラグと `current_phase.txt` のチェックを追加。
- **集中モードの最小化記録の重複防止。** `FocusModeMinimize()` が `g.focusMinimizedHwnds` に同じウィンドウハンドルを重複して積み続けることがあり、`FocusModeRestore()` の処理が肥大化する原因になり得たため、`HasVal()` で重複チェックを追加。
- **作業達成チェックが中休み中に機能しないバグを修正。** `CheckActiveWork()` の先頭ガードが `g.phase != "lock"` の場合に即returnしていたため、その後にある「中休み中に達成した場合は即座にボタン表示」という分岐が実装上到達不可能（デッドコード）だった。ガード条件に `intermission` を追加し、中休み中も計測・達成チェックが機能するようにした。
- **休憩中・中休み中の終了通知を追加。** これまで `CleanupPhaseFile()` は「ロック中の終了」のみをDiscord通知していたが、休憩・中休み中に終了した場合は作業ノルマ未達成でも一切通知されず、サボりの抜け道になっていた。作業ノルマ（`totalWorkGoalMinutes`）達成済みなら休憩中の終了を許可（通知なし）、未達成なら休憩・中休み中でもロック中と同様に通知するよう変更。
- **作業ノルマ残り時間の表示機能を追加。** lock_window.ahk自身のトレイアイコンのツールチップに「🎯 ノルマまであと ○時間○分」を表示するようにした。launcherのトレイツールチップ（クロッキー開始までの待機時間表示）と競合しないよう、lock_window.ahk側の別トレイアイコンを利用。休憩中も直近の値を表示し続ける。
- **クロッキー→通常作業をまたぐと作業ノルマがリセットされるバグを修正。** クロッキーは launcher.ahk が別プロセスとして起動し、終了後にまた別プロセスとして通常作業（`/auto`）を起動する。`g.activeWorkMs` はメモリ上の値のみで管理されていたため、クロッキー用プロセスが終了すると進捗が失われ、通常作業が0から再スタートしていた。`work_goal.txt`（`game_limit.txt` と同じ日付ベースの読み書き方式）に都度保存し、プロセスをまたいで引き継がれるように修正。
- **中休み中をサボり検知の対象から除外。** `CheckAbsence()` は `lock` に加えて `intermission`（中休み）も監視対象にしていたため、中休み中に何も操作しないと「あと1分でサボり判定になります」の警告が飛んでいた。運動・食事休憩と同様、中休みも監視対象から除外し、休憩中に無操作でも通知が飛ばないよう修正。
- **週次レポートの日付ソートで実行時エラーが発生するバグを修正。** `dateList[j-1] > dateList[j]` という "-" 区切りの日付文字列同士の直接比較が、実際には `Error: Expected a Number but got a String` を投げることが判明（仕様書には以前から「`StrCompare` を使用」と書かれていたが、実装では反映されていなかった）。`StrCompare()` を使った明示的な文字列比較に修正し、仕様書の記載と実装を一致させた。
- **休憩中に昼休み等を使うと、復帰直後に離席時間が丸ごとサボりとして誤検知されるバグを修正。** `A_TimeIdlePhysical`（物理的な無操作時間）は休憩・昼休み・運動でサボり監視の対象外になっている間も裏で増え続けており、`CheckAbsence` が監視を再開した直後にその蓄積分をそのまま読み取ってしまっていた（例：休憩中に昼休みを使い、その間PCを離れ、休憩終了後にlockが始まった時点で、離れていた時間がまとめてサボりと判定される）。`lock` フェーズに入った瞬間の `A_TickCount` を記録し、無操作時間をその時点からの経過時間でキャップすることで、監視対象外フェーズ中の離席が誤って計上されないよう修正。
- **週次レポートのテスト送信機能とログ出力を追加。** これまでPowerShell側の送信結果（成功・失敗・レスポンス内容）は `Write-Host` でしか出力されておらず、`RunWait` の非表示実行のため実質握りつぶされていて、不具合の切り分けが困難だった。`croquis_report_log.txt` に送信結果を都度記録するようにし、`launcher.ahk /test-croquis-report` で土曜を待たずにその場でテスト送信できるようにした（結果はMsgBoxで表示）。
- **`test_croquis_upload.ahk` を新規追加。** `/test-croquis-report` はlauncher.ahk本体を巻き込む（`#SingleInstance Force` で再起動される・本物の画像を送信してしまう）ため日常のデバッグには面倒との指摘を受け、launcher.ahkを一切起動せずダミー画像だけで「日付ソート→Discord multipart送信」を検証できる独立スクリプトを新設。Webhook URLはlauncher.ahkから読み取るため二重管理にはならない。
- **【重要】週次レポートが送信されない根本原因を特定・修正（その1）。** `test_croquis_upload.ahk` でのテスト中、PowerShell側のログが一切出力されない（＝PowerShellスクリプト自体がパースエラーで実行開始すらできていない）ことが判明。原因は `$payload = ...::new("{"content":"..."}"` という手動JSON組み立てで、JSON用の `"` とPowerShell文字列を区切る `"` が衝突し、生成された `.ps1` が構文エラーになっていたこと。手動でのJSON文字列組み立てをやめ、PowerShell側の `ConvertTo-Json` にJSON化を任せる形に修正。
- **【重要】根本原因を特定・修正（その2）。** その1の修正後もPowerShell側のログが一切出ない症状が続いたため再調査。`.ps1` ファイルを `"UTF-8-RAW"`（BOMなし）で書き出していたが、スクリプト中に日本語（絵文字・メッセージ文）を含むため、BOMが無いとWindows PowerShell(5.1)がファイルの文字コードをUTF-8と判断できず、システムのANSIコードページとして誤読し、文字列リテラルが壊れてパースエラーになっていた。週次レポート関連の `.ps1` 書き出しのみ `"UTF-8"`（BOMあり）に変更。この2つが重なって「クロッキー一斉アップロードがここしばらく動いていなかった」問題の本当の原因だった可能性が高い。
- **クロッキーのモデル画像フォルダをモードごとに分離。** これまで全モード共通で `croquis_models` フォルダから画像を選んでいたが、モード1は `croquis_models_1`、モード2は `croquis_models_2`、モード3は `croquis_models_3` というように、モードごとに専用フォルダから選ぶように変更。`croquisModeParams` の各モード定義に `folder` を追加し、`/croquis:lockSecs:sets:interSecs:mode` の第5引数でモード番号をlock_window.ahkにも渡すようにした（セット間の次画像選択でも同じモード専用フォルダを参照するため）。使用済み画像リストは `croquis_used.txt` に**全モード共通で一元管理**し、モードが違っても同じ画像が二度使われないようにした（あるモードの画像を使い切った際は、共有リストから該当モードの画像分だけを取り除いてリセットし、他モードの履歴は保持する）。**利用にはフォルダの新規作成と画像の振り分けが必要**（下記「注意」参照）。
- **クロッキーにモード4（記憶描画モード）を追加。** 時間割はモード2と同じ（15分×2セット）だが、2セット目は新しい画像を選ばず、1セット目のモデルを記憶を頼りに描く練習用モード。`StartCroquisInterSet()` で `croquisArg.mode = 4` の場合のみ `PickNextCroquisImage()`/`CopyNextCroquisImage()` の呼び出しをスキップする形で実装。画像プールはモード2と共用（`croquis_models_2`）なのでフォルダの新規作成は不要。
- **クロッキーにランダムモード（`croquisMode = 0`）を追加。** 設定すると、起動のたびに `croquisModeParams` に登録済みの既存モード（1〜4）からランダムに1つ選んで実行する。`croquisMode` の設定値自体は書き換わらず、毎回その場でランダムに選び直す。選ばれたモードはTrayTipで通知される。
- **ランダムモードにモード除外機能を追加。** `croquisRandomExcludeModes`（配列）で指定したモード番号は、ランダムモードの選択肢から除外されるようにした（例: `[4]` でモード4を除外）。除外しすぎて選べるモードが無くなった場合は除外自体を無視するフォールバックつき。
- **settings_editor.htmlに配列型（`arr`）の編集対応を追加。** これまで `int`/`select`/`str` にしか対応しておらず配列は編集できなかったが、`croquisRandomExcludeModes` を編集できるようにするため新設。画面上は `3, 4` のようなカンマ区切り文字列として編集し、保存時にAHKの配列リテラル `[3, 4]` へ自動変換する。
- **クロッキーのデフォルト設定を変更。** `croquisMode` を `2` → `0`（ランダムモード）、`croquisRandomExcludeModes` を `[]` → `[2]`（モード2を除外）に変更。以後この設定をデフォルトとして扱う。
- **作業時間の計測から無操作時間を除外する機能を追加。** これまで作業ウィンドウがアクティブでありさえすれば経過時間をそのまま加算していたため、ウィンドウを開いたままスマホを触っている時間なども作業時間としてカウントされてしまっていた。`workIdleToleranceSecs`（デフォルト60秒）以上 `A_TimeIdlePhysical`（無操作時間）が経過している場合は、その間を作業時間に加算しないよう `CheckActiveWork()` を修正。
- **無操作時にロックタイマー自体も自動で一時停止するように変更。** 作業時間への未加算だけでなく、`workIdleToleranceSecs` 以上無操作ならロックタイマー（クロッキー含む）自体を一時停止する `CheckIdleTimerPause()` を新設（0.2秒ごとに監視）。操作再開は5秒間隔の `CheckActiveWork` を待たず、この0.2秒間隔のチェックで即座に検知して再開する。食事休憩・運動など既存の一時停止の仕組みと衝突しないよう `g.idlePaused` フラグで区別し、他の一時停止の開始・解除処理すべてにこのフラグのクリアを追加した。
- **クロッキーの撮影待機中に離席すると、以降タイマーが永久に止まったままになるバグを修正。** 撮影待機（`CaptureCountdownTick`）は `g.phase = "lock"` のまま `g.isPaused` を見ずに独自に時間管理しているため、上記の無操作自動一時停止機能が撮影待機中に発動すると、`g.isPaused` が立ったまま次のフェーズ（次のセットまでの休憩）に引き継がれ、その後永久に解除されなくなっていた（無操作解除の判定が `g.phase = "lock"` の間しか働かないため）。`g.inCaptureWait` フラグを新設し、撮影待機中は無操作による一時停止の対象から完全に除外するよう修正。
- **外出モード（🚪）を追加。** 数時間単位の外出のために、現在のフェーズ（ロック・休憩・中休み・クロッキー問わず）を無期限に一時停止できるボタンを新設。AHKの `Suspend`/`Pause` は使わず（`A_TickCount` が経過し続けてしまい、復帰直後にタイマーが尽きたと誤判定される問題があるため）、既存の食事休憩・運動と同じ「残り時間を保存し、再開時にそこから数え直す」方式で実装。ボタン行は5個に増えたため、各ボタンの幅を70px→56pxに縮小して対応した。
- **画面オフのまま寝落ちすると起床ルーティンが始まらない問題に対応。** PCがスリープせず画面オフのままの状態で長時間無操作になった場合、実際のスリープイベントが発生しないため `PBT_APMRESUMEAUTOMATIC` を起点とする起床処理が一切動かなかった。`A_TimeIdlePhysical` を1分ごとに監視し、`minSleepHours` 以上の無操作の後に操作再開を検知した瞬間を「スリープ復帰」とみなして `StartWakeRoutine()` を起動する `CheckLongIdleWake()` を新設。実際のスリープ復帰処理とは `wakeRoutineActive` 等で二重発火しないようにしている。
- **作業ノルマ達成後の超過分を、翌日のノルマから差し引く機能を追加。** ノルマ達成後にさらに作業した場合、その超過分は翌日1日だけノルマから差し引かれる（`workGoalMinMinutes`＝90分を下限とし、それ以上は翌々日以降に持ち越さない）。`totalWorkGoalMinutes`（通常時の設定値）はそのままに、実際に使うノルマを `effectiveWorkGoalMinutes`（起動時に前日分から算出）として分離し、達成判定・残り時間表示の基準を全てこちらに変更。`work_goal.txt` に本日の実効ノルマも保存し（4項目目）、翌日の繰り越し計算に使えるようにした。
- **【要望】ノルマ達成後、lock_window.ahk終了後にロック外で行った作業（残業）を計測する機能を追加。** これまで作業ノルマの進捗（`work_goal.txt`）は lock_window.ahk が `lock`/`intermission` フェーズ中にしか更新できず、ノルマ達成後に休憩中スクリプトを終了して1日を終える運用の場合、終了後にロック外で行った追加作業は一切計測されなかった。launcher.ahk側に `CheckOvertimeWork()`（5秒ごと）を新設し、「本日ノルマ達成済み・現在lock_window.ahkがlock/中休み中ではない・作業ウィンドウがアクティブかつ無操作許容時間内」の間だけ経過時間を `overtime_work.txt` に加算するようにした。二重計上防止のため、lock_window.ahk が計測中のフェーズ（lock/intermission）は対象外にしている。翌日、lock_window.ahk起動時にこのファイルの前日分を読み込み、既存の超過分計算（`_overMin`）に加算する。判定に使う「対象ウィンドウ」「無操作許容時間」は lock_window.ahk の `workWindowTitles`/`workWindowProcesses`/`workIdleToleranceSecs` と同じ内容を launcher.ahk 側にも複製して使用（`overtimeWorkWindowTitles`/`overtimeWorkWindowProcesses`/`overtimeIdleToleranceSecs`）しており、**両ファイルで値を合わせておく必要がある。**
- **【要望】ノルマ・残業の「1日」の区切りを、カレンダー日付（0時）から睡眠復帰ベースに変更。** 上記の残業計測機能を実装する過程で、「休憩中にスクリプトを終了し、日をまたいで残業を続けた場合、0時を過ぎた瞬間に残業が計測されなくなる」という問題が判明した（`work_goal.txt`の日付更新はlock_window.ahk起動時にしか起きないため、日付が変わった直後は「本日ノルマ達成」の記録自体が存在しない扱いになっていた）。利用者の意図は「睡眠が1日の区切りであり、カレンダー上の日付は関係ない」というものだったため、根本的に区切りの基準を変更した。具体的には：
  - launcher.ahk に `dayId`（1から始まる通し番号）を導入し、`day_state.txt` に保存する。書き込むのは launcher.ahk のみ。
  - `dayId` は `StartWakeRoutine()`（自動スリープ復帰・長時間無操作からの復帰・手動でのスリープ解除ルーティン実行、いずれの経路でも共通）が呼ばれるたびに1つ進める。つまり「しっかり睡眠をとって起きるまでは同じ1日」という扱いになる。
  - `work_goal.txt`・`overtime_work.txt` の1項目目を、カレンダー日付（`yyyyMMdd`）からこの `dayId` に変更。lock_window.ahk起動時の前日繰り越し判定も「`dayId` が同じなら同日」「`currentDayId - 1` なら前日」という単純な整数比較に変更した。
  - **この変更の対象は work_goal.txt / overtime_work.txt（作業ノルマ・残業）のみ。** 運動・昼休みボタンの1日1回制限（`exercise_log.txt`/`lunch_log.txt`）、ゲームプレイ時間制限（`game_limit.txt`）、サボりログの23時書き出し、週次レポートの曜日判定は今回の変更対象外で、**従来通りカレンダー日付基準のまま**（意図的にスコープを限定した。同じ問題が理論上はこれらにも当てはまるため、必要になれば同じ仕組みを適用できる）。
  - 既存の `work_goal.txt`/`overtime_work.txt`（更新前の `yyyyMMdd` 形式）が残っていた場合、`dayId` との比較が一致しないため「記録なし」として安全側に無視され、以後は新形式で保存し直される（クラッシュ等は起きない）。

---

## ファイル構成

| ファイル | 役割 |
|---|---|
| `lock_window.ahk` | 作業タイマー本体。手動またはランチャーから起動 |
| `launcher.ahk` | スタートアップ常駐。スリープ復帰検知・クロッキー・サボり監視・就寝ブロック |
| `settings_editor.html` | 設定値をブラウザで編集し書き換え済みファイルをダウンロードするツール |
| `test_croquis_upload.ahk` | クロッキー週次レポートの「Discord一斉アップロード」機能を、ダミー画像で単体テストするための独立スクリプト（launcher.ahkは起動しない。詳細は下記） |

---

## launcher.ahk の機能

### スリープ復帰検知・起動フロー

1. スリープ時間が `minSleepHours`（6時間）以上かつ `skipWDays`（土曜）でなければ起動
2. 復帰から `delayMinutes`（30分）後にカウントダウン終了
3. 食事時間帯（19:00〜20:30）内であればクロッキーを食事休憩終了まで待機
4. クロッキーセッション開始（`lock_window.ahk /croquis:...` を起動）
5. クロッキー終了後（`current_phase.txt` が `croquis_done` になったことを2秒ごとに検知）、通常タイマー起動（`lock_window.ahk /auto`）

- スリープ前に `last_sleep.txt` に時刻を記録（30分以内の短時間スリープは上書きしない）
- 復帰後にカウントダウンGUIを表示（240×120px、右に「今すぐ開始」ボタン付き）
- トレイアイコンのツールチップにも残り時間を表示
- トレイアイコンのダブルクリックでGUIを最前面に戻せる

**手動実行（トレイメニュー「🔁 スリープ解除ルーティンを手動実行」）**

バグや寝落ちでスリープ復帰イベントが検知されなかった場合などのために、上記2〜5（待機→クロッキー→作業）を手動で開始できる。自動起床時と共通の `StartWakeRoutine()` を呼ぶため流れは完全に同じだが、`skipWDays` や `minSleepHours` によるスキップ判定は行わない（手動実行は常に開始する）。以下の場合は実行されない：
- ルーティンが既に進行中（`wakeRoutineActive` フラグ）
- `lock_window.ahk` が既に起動中（`current_phase.txt` が空でない）

いずれの場合もTrayTipで理由を通知するのみで、何も開始しない。

**無操作時間ベースの起床検知（`CheckLongIdleWake`・1分ごと）**

画面がオフになるだけでPC自体はスリープしない電源設定の場合、そのまま無操作で長時間経過（寝落ち等）しても `PBT_APMSUSPEND`/`PBT_APMRESUMEAUTOMATIC`（実際のスリープ・復帰）が一切発生せず、ルーティンが永遠に始まらないことがある。これを補うため、`A_TimeIdlePhysical`（物理的な無操作時間）を1分ごとに監視し、`minSleepHours` 以上無操作が続いた後、操作が再開された瞬間（idle時間が閾値未満に戻った瞬間）を「実際のスリープ復帰」と同じ意味とみなし、`StartWakeRoutine()` を起動する。

- 無操作が閾値を超えている間は `longIdleDetected := true` を立てるだけで、まだ何も開始しない（本人がまだ戻ってきていないため）
- 操作再開を検知した時点で初めてルーティンを開始する
- `wakeRoutineActive`（多重発火防止）・`current_phase.txt`（lock_window.ahk起動中でないか）・`skipWDays` を実際のスリープ復帰と同様にチェックする
- 実際にPCがスリープした場合はこのタイマー自体もその間停止する（AHKプロセスごと一時停止するため）ので、通常のスリープ復帰処理と二重に発火することはない

### 睡眠ベースの日付境界（dayId・`day_state.txt`）

作業ノルマ（`work_goal.txt`）・残業時間（`overtime_work.txt`）における「1日」の区切りは、カレンダー上の日付（0時）ではなく、ここで管理する `dayId`（1から始まる整数の通し番号）を基準にする。

- `dayId` は `day_state.txt` に保存する。**書き込むのは launcher.ahk のみ**（lock_window.ahk は自分の起動時に読み取るだけで、以後そのセッション中は再読み込みしない）
- `StartWakeRoutine()`（自動スリープ復帰・`CheckLongIdleWake` による長時間無操作からの復帰・手動でのスリープ解除ルーティン実行、いずれの経路で呼ばれた場合も共通）の先頭で `dayId` を1つ進める
- つまり「しっかり睡眠をとって起きるまでは同じ1日」という扱いになり、深夜0時をまたいで作業・残業を続けても、その日の続きとして正しく計測される
- 初回起動時（`day_state.txt` が存在しない場合）は `dayId = 1` から開始する
- **この仕組みの対象は work_goal.txt / overtime_work.txt のみ。** 運動・昼休みボタンの1日1回制限、ゲームプレイ時間制限、サボりログの23時書き出し、週次レポートの曜日判定は対象外で、従来通りカレンダー日付基準のまま（意図的にスコープを限定している）

### 残業時間の計測（ロック外での作業ノルマ超過分）

lock_window.ahkは、ノルマ達成後に休憩中スクリプトを終了して1日を終える運用を想定しているため、終了後にロック外で行った追加の作業時間はlock_window.ahk側では計測できない。そのぶんを launcher.ahk が常駐監視で独自に計測し、翌日のノルマ軽減（lock_window.ahk側の繰り越し計算）に加算する。

- `CheckOvertimeWork()`（5秒ごと）が、以下をすべて満たす間だけ経過時間を加算する：
  - 本日（`currentDayId`）の `work_goal.txt` が既にノルマ達成済み（`goalReached=1`）
  - `current_phase.txt` が `lock`／`intermission` ではない（その間はlock_window.ahk側で計測済みのため、二重計上防止）
  - 対象ウィンドウ（`overtimeWorkWindowTitles`/`overtimeWorkWindowProcesses`）がアクティブ、かつ無操作許容時間（`overtimeIdleToleranceSecs`）内
- 結果は `overtime_work.txt` に `dayId|残業ms` 形式で保存する
- `overtimeWorkWindowTitles`/`overtimeWorkWindowProcesses`/`overtimeIdleToleranceSecs` は、lock_window.ahk側の `workWindowTitles`/`workWindowProcesses`/`workIdleToleranceSecs` と**必ず同じ内容にする必要がある**（launcher.ahk側に複製して保持しているため、判定基準がずれると正しく計測できない）
- `dayId` が変わった際、`overtime_work.txt` への書き込みは即座には行わず、メモリ上のカウンタだけリセットする。ファイルへの書き込みは「本日分の残業が実際に発生したとき」まで遅延させる（日をまたいで作業を続けていた場合に、まだlock_window.ahkに読まれていない前日分をファイルごと消してしまわないための対策）
- このファイルは lock_window.ahk 起動時の翌日繰り越し計算でのみ読み込まれる（launcher.ahk側では読み込み後のリセット等は行わない）

### クロッキー機能

- モード選択（`croquisMode`）: 1=25分×1枚 / 2=15分×2枚 / 3=5分×5枚 / 4=15分×2枚（2セット目は記憶描画） / 0=ランダムモード
- 各モードのパラメータは `croquisModeParams` Map で定義（lockSecs/sets/interSecs/**folder**）
- **モードごとに別々のモデル画像フォルダを使用。** `croquis_models_1`（モード1）／`croquis_models_2`（モード2・モード4も共用）／`croquis_models_3`（モード3）を事前に作成し、それぞれに画像を入れておく（フォルダは手動作成・画像も手動で振り分ける）
- パラメータを `/croquis:lockSecs:sets:interSecs:mode` の形式で lock_window.ahk に引数渡し（**mode** はセット間の次画像選択でどのフォルダを使うか・画像を選ぶかどうかの判定に必要）
- モデル画像をそのモード専用フォルダからランダム選択し、GDI+でクリップボードにコピー（ビューアは開かない）
- 使用済み画像は `croquis_used.txt`（**全モード共通の1ファイルで一元管理**）に記録する。モードが違っても同じ画像が二度使われることはない。あるモードの画像を全て使い切ると、共有リストから「そのモードの画像分だけ」を取り除いてリセットする（他モードの使用履歴は保持されるため、他モードの画像が誤って早期に再利用されることはない）
- 画像フォルダが存在しない・画像がない場合はクロッキーをスキップして通常タイマーへ
- **モード4（記憶描画モード）：** 時間割はモード2と同じ（15分×2セット、セット間2分休憩）。1セット目は通常どおり画像を選んで描き、**2セット目は新しい画像を選ばない**（`StartCroquisInterSet()` 内で `croquisArg.mode = 4` の場合、`PickNextCroquisImage()`/`CopyNextCroquisImage()` の呼び出しをスキップ）。1セット目のモデルを記憶を頼りに描く練習用。画像プールはモード2と共用（`croquis_models_2`）
- **ランダムモード（`croquisMode = 0`）：** `LaunchCroquis()` の冒頭で `croquisModeParams` に登録されている既存モード（現在1〜4）からランダムに1つ選び、そのモードとして起動する。`croquisMode` の設定値自体は書き換えない（起動のたびに毎回ランダムに選び直す）。どのモードが選ばれたかはTrayTipで通知される
- **`croquisRandomExcludeModes`：** ランダムモードの選択肢から除外したいモード番号を配列で指定（例: `[4]` でモード4を除外）。除外した結果、選べるモードが0個になった場合は除外を無視して全モードから選ぶ（安全側フォールバック）

### クロッキー週次レポート

- 毎週土曜15時に `croquis_shots` フォルダの過去7日分の画像をDiscordに送信
- 送信先: `croquisReportWebhook`（サボり通知とは別のWebhook URL設定済み）
- テキスト（週・枚数・実施日一覧）＋画像をmultipart/form-dataで送信（最大10枚）
- PowerShellを `RunWait` で同期実行し、完了後にTrayTipを表示
- 日付の昇順ソートは `StrCompare` を使用（数値比較エラーを避けるため）
- **送信結果は `croquis_report_log.txt` に都度上書き保存**（画像読込の成否・HTTPステータス・失敗時のレスポンス内容・例外メッセージ）。PowerShell側の `Write-Host` は `RunWait` が非表示実行のため画面に出ず握りつぶされていたため、ファイルlog化して可視化した
- `launcher.ahk /test-croquis-report` でも土曜を待たずその場でテスト送信できる（`SendCroquisWeeklyReport(true)`）が、`#SingleInstance Force` により実行中のlauncher.ahkが一度終了して置き換わるうえ、`croquis_shots` 内の本物の画像を実際に送信してしまうため、日常的な動作確認には次項の `test_croquis_upload.ahk` の方が手軽
- **`test_croquis_upload.ahk`（独立スクリプト）でlauncher.ahkを一切起動せずに単体テスト可能。** launcher.ahkから `croquisReportWebhook` の値だけをその場で読み取り、ダミーのPNG画像（4x4px・直近7日以内にランダムな日付を割り当てて複数生成）を使って「日付ソート→multipart送信」の実処理を検証する。結果は `test_croquis_upload_log.txt` に保存され、完了後MsgBoxで表示。送信自体は本番のWebhookに対して行われる（メッセージ本文に「テスト送信」と明記）。実行後、生成したダミー画像は自動削除される

### サボり監視（CheckAbsence・30秒ごと）

- `current_phase.txt` が `lock` のときのみ監視（**中休みは除外** — 休憩中に休めないと意味がないため）
- 監視対象外フェーズ（lunch/exercise/break/intermission等）に入ったら sabo 状態を全リセット（isAbsent/warnedThisIdle/absentStartMs）
  → これにより食事休憩・中休み等での無操作が復帰後にサボりとして誤記録されるのを防ぐ
- **`A_TimeIdlePhysical`（無操作時間）は今回の `lock` 開始時点からの経過時間でキャップする**（`sabo.lockStartTick`）。休憩・昼休み・運動中に離席していた時間は `A_TimeIdlePhysical` にそのまま残り続けるため、これをキャップしないと lock 再開直後に「休憩中に離席していた分」まで丸ごとサボりとして誤検知してしまう
- `absentThresholdMin`（15分）無操作でサボり判定
- 判定1分前にTrayTip通知＋`saboWarnWebhook`（専用Webhook）にDiscord送信
- 復帰時に`discordWebhook`（通常Webhook）にDiscord通知（離席時間を記録）
- 毎日23時に `sabo_logs/yyyy-MM-dd_sabo.txt` にログ書き出し
- 翌朝スリープ解除時に前日ログをGUI表示
- 異常終了（sentinel ファイルで次回起動時に検知）はログに記録

### Discord通知タイミング（launcher側）

- サボり警告（閾値1分前）→ `saboWarnWebhook` に送信
- サボり復帰確認（離席N分）→ `discordWebhook` に送信
- launcher.ahk 終了時（リロード時は通知しない）→ `discordWebhook` に送信

### 就寝スマホブロック

- トレイメニュー「🌙 就寝モード開始」→ 10秒後にモニターOFF、`nightBlockDelay`（30分）後にNextDNSでスマホをブロック＋PCをスリープ
- 翌朝スリープ解除時（6時間以上経過）に `night_block_flag.txt` を確認しNextDNSブロック解除
- 就寝ブロックは手動トリガーのみ（旧・自動スケジュール方式の関連コードは削除済み）

---

## lock_window.ahk の機能

### 起動モード

- **自動起動**（`/auto` 引数）：GUIなしで即スタート。siteList全件＋Steamをブロック
- **クロッキーモード**（`/croquis:lockSecs:sets:interSecs` 引数）：クロッキー専用タイマー。下記参照
- **手動起動**：GUIで Lock時間・Break時間・セット数を選択してStart

### クロッキーモード（/croquis）

- 引数形式: `/croquis:1500:1:0:2`（lockSecs:sets:interSecs:mode）
- 集中モード強制ON・NextDNSブロック・GUIを紫色で表示
- セット数が複数の場合（モード2・3）:
  - 各セット終了 → 撮影猶予カウントダウン（`croquisCaptureWait`=180秒）→ キャプチャ → NextDNS解除 → セット間休憩 → 次の画像をクリップボードにコピー → 次セット開始
  - **注意**: `CaptureCountdownTick` はネスト関数のクロージャ問題を避けるためトップレベル関数として定義し、必要な変数は `croquisCaptureEnd` 等のグローバル変数で受け渡している
- 全セット終了後 → 最終休憩（`croquisBreakSecs`=600秒）→ `WritePhase("croquis_done")` → `Sleep(8000)` → `ExitApp()`
- launcher.ahk 側が2秒ごとに `current_phase.txt` を監視して `croquis_done` を検知し通常タイマーを起動
- `CleanupPhaseFile`（OnExit）: `current_phase.txt` の実際の内容（`actualPhase`）を読み、`croquis_done` のときはファイルを消去しない

### キャプチャ（CaptureCroquisResult）

- タイマー終了の撮影猶予中に自動実行（ユーザー操作なし）
- CLIPStudioPaintを前面に強制表示（0.8秒待機）してからウィンドウキャプチャ
- CLIPStudioが見つからない場合はプライマリモニター全体にフォールバック
- 保存先: `croquis_shots\yyyy-MM-dd.png`（同日2枚目以降は `yyyy-MM-dd_2.png` と連番）
- PowerShellを `RunWait` で同期実行

### 次セット間の画像選択（lock_window.ahk 側）

- `PickNextCroquisImage()` / `CopyNextCroquisImage()` がlauncher側と同じロジックで画像を選択
- 画像フォルダは `A_ScriptDir\croquis_models_<mode>`（起動引数で受け取った `croquisArg.mode` から判定。旧バージョンのlauncher.ahkからmode無しで起動された場合はモード1として扱う）
- 使用済みリストは `croquis_used.txt`（launcher側と共有・全モード共通の1ファイル）

### ポモドーロタイマー（/auto・手動）

- ロック → 休憩 → ロック … を指定セット数繰り返す
- セット完了後は「中休みモード」（15分）に移行
- 中休みで「作業完了」ボタンを押すと本当のdoneモードへ（**作業達成済みのときのみ表示**）
- 中休みタイムアウト → 2セット追加して再開
- ➕ボタンでセット追加（中休み・done中でも再開可能）
- `RunPomodoro` は generation カウンターでタイマー競合を防止

### ウィンドウブロック

- ロック中、`siteList` に含まれるタイトルのウィンドウを300msごとに最小化
- ロック開始/終了時に NextDNS でスマホのSNS・動画・ゲーム系もブロック/解除

### 一時停止系モード（タイマー・サボり検知・ブロックが停止）

| モード | トリガー | 時間 |
|---|---|---|
| 🍽️ 食事休憩 | 毎日19:00〜20:30 自動 | 固定 |
| 🚴 運動モード | ボタン（1日1回） | 60分 |
| 🥗 昼休みモード | ボタン（1日1回） | 50分 |
| 🚪 外出モード | ボタン（回数無制限） | 無期限（手動再開まで） |

- これらは `g.isExercise` フラグを共用し `ResumeAfterExercise()` で復帰（外出モードのみ独自の仕組み）
- 食事休憩開始時: `WritePhase("lunch")` でサボり検知を無効化、`timerCount.Visible := false` で数字表示を隠し代わりに「⏭ 食事休憩を終了」ボタン（y=38, w=160, h=36）を表示
- 食事休憩の手動終了ボタン（`OnMealEnd`）: 押すと通常の食事休憩終了と同じ処理を実行し `timerCount.Visible := true` に戻す
- 食事休憩終了時: `WritePhase(g.phase)` で元のフェーズに戻し、NextDNSブロックを復帰
- `ResumeAfterExercise()` の食事休憩中分岐でも `NextDnsUnblock()` と `timerCount.Visible := true` を実行
- 一時停止中に `g.isPaused = true` になると `g.lastActiveCheck` が更新されないため、復帰時に `g.lastActiveCheck := 0` をリセット（食事/運動どちらも）→ 作業時間への誤加算防止
- **🚪 外出モード（`OnGoOut`）：** 数時間単位の外出のために、現在のフェーズ（ロック・休憩・中休み・クロッキー問わず）を無期限に一時停止するボタン。仕組みは食事休憩・運動と同じ（`g.isPaused` ＋ `g.pausedRemainingMs` による残り時間の保存・復元）だが、AHKの `Suspend`/`Pause` は使っていない（これらはタイマーの発火自体を止めるだけで、`A_TickCount`（システム経過時間）は止められないため、再開時に無操作時間がそのまま経過扱いになり「戻ってきた瞬間にタイマーが尽きる」問題が起きる。既存の一時停止と同じ「残り時間を保存し、再開時にその場から数え直す」方式で回避している）。ボタンは同じものをトグルし、開始時「▶ 再開」に変わる。食事休憩・運動モードと同時には使えない（互いの終了処理が競合するため、開始しようとするとTrayTipで案内して弾く）

### 集中モード（🎯）

- 許可リスト（`focusModeAllowList`/`focusModeAllowProcesses`）以外の全ウィンドウを最小化
- **自動発動**：`focusModeAutoFromSet`（3）セット目以降、`focusModeAutoChance`（30%）の確率でON。そのセットのみ有効（auto フラグで管理）
- **手動発動**：ロック中にボタンON → 休憩中のみOFF可能 → OFFしなければ次セットへ引き継ぎ
- 発動時 `focusModeCountdownSecs`（15秒）の猶予あり（猶予中は通常ブロックも一時停止）
- 終了時に最小化したウィンドウを復元（`focusMinimizedHwnds` に記録）

### 休憩延期

- ロック終了時に `workWindowProcesses`（CLIPStudio等）がアクティブなら休憩を延期
- 偽装タイマー（`breakDeferFakeMinutes`=3分）を表示（0になったらリセット繰り返し）
- 作業ウィンドウ以外が `breakDeferGraceSecs`（5秒）以上続いたら休憩開始

### 作業達成時間

- `totalWorkGoalMinutes`（150分=2時間半）の間、`workWindowProcesses` がアクティブになると達成
- **超過分の翌日繰り越し。** ノルマ達成後もさらに作業した場合、その超過分は翌日1日だけノルマから差し引かれる（例: 前日30分超過なら翌日のノルマは150分→120分）。ただし `workGoalMinMinutes`（90分=1時間30分）を下回ることはない。差し引ける範囲を超えた超過分は翌々日以降に持ち越さない。実際に使うノルマは `effectiveWorkGoalMinutes`（起動時に算出）で、達成判定・残り時間表示など全てこちらを基準にする（`totalWorkGoalMinutes` は「通常時のノルマ」設定値のまま変化しない）
- **超過分には、lock_window.ahk終了後にロック外で行った「残業」時間（launcher.ahk計測・`overtime_work.txt`）も含まれる。** 詳細は launcher.ahk 側「残業時間の計測」の項を参照
- **`workIdleToleranceSecs`（60秒）以上マウス・キーボードの操作が無い場合、作業ウィンドウがアクティブなままでもその間は計上しない**（`A_TimeIdlePhysical` で判定。ウィンドウを開いたままスマホを触っている時間などを除外するため）
- **無操作時はロックタイマー自体も自動で一時停止する（`CheckIdleTimerPause`・0.2秒ごと）。** `g.phase = "lock"`（クロッキー含む）中に `workIdleToleranceSecs` 以上無操作になると、`g.isPaused` を立ててタイマーを完全に停止し、画面表示も「⏸ 離席検知中」に切り替える。操作を検知すると（次のCheckActiveWorkの5秒待ちではなく）0.2秒間隔のこのチェックで即座に検知して再開し、表示も元に戻す。中休みは対象外（無操作でもタイマーが進み続けることで自動セット追加が機能するため）。食事休憩・運動など他の理由による一時停止とは `g.idlePaused` フラグで区別しており、互いに干渉しない
- **クロッキー中も計測される**（`CheckActiveWork` タイマーはスクリプト起動時から常時動作し、`g.phase = "lock"` または `"intermission"` かつ `g.isPaused = false` なら計測）
- 達成時：TrayTip通知（Discord通知は送らない）
- 達成前：中休み中の「✅ 完了」ボタンは `workDoneBtn.Visible := false`（非表示）
- 達成後に中休みに入った場合は即座に表示。中休み中に達成した場合も即座に表示
- 達成状況（`g.workGoalReached`）は休憩中・中休み中のスクリプト終了時の通知可否にも使われる（下記「Discord通知タイミング」参照）
- **ノルマ残り時間はlock_window.ahk自身のトレイアイコンのツールチップに表示**（`A_IconTip`）。「🎯 ノルマまであと ○時間○分」、達成後は「🎯 本日の作業ノルマ達成済み」。`CheckActiveWork`（5秒ごと）のたびに更新され、休憩（5分休み）中など更新が走らない間も直近の値を表示し続ける
- **`work_goal.txt` に「1日の区切りID（dayId）」＋累計時間＋達成フラグ＋本日の実効ノルマを都度保存し、プロセスをまたいで引き継ぐ。** クロッキーは launcher.ahk が `/croquis` 引数で別プロセスとして起動し、終了後にまた別プロセスとして `/auto` を起動するため、`g.activeWorkMs` はメモリ上の値だけでは引き継がれない（クロッキー用プロセスが終了すると消える）。`CheckActiveWork` のたびにファイルへ保存し、スクリプト起動時（`g` の初期化時）にその日の分を読み込むことで、クロッキー→通常作業の切り替えをまたいでもノルマ進捗が正しく引き継がれる。フォーマットは `dayId|activeWorkMs|reached(0/1)|effectiveGoalMin`。
  - **dayId は「カレンダー日付」ではなく「睡眠復帰を1つの区切りとする通し番号」。** launcher.ahk が管理し `day_state.txt` に保存する（詳細は launcher.ahk 側「睡眠ベースの日付境界（dayId）」の項を参照）。lock_window.ahk は起動時にこの値を読み取って `currentDayId` として使うだけで、以後そのプロセスが動いている間は再読み込みしない（＝起動中に睡眠を挟んでも、そのセッション自体は日付境界の影響を受けない）
  - 起動時に読み取った `currentDayId` と `work_goal.txt` の1項目目を比較し、**同じなら継続、`currentDayId - 1` なら前日として超過分（＋残業時間）を計算して本日の実効ノルマに反映、それ以外（2日以上前・記録なし）なら繰り越しなしで通常のノルマを使う**

### ゲームプレイ時間制限

- `gameLimitMinutes`（120分）を超えると `WinClose` で通常終了＋以降最小化し続ける
- 制限後、作業セットを `gameLimitExtendSets`（1セット）完了すると1日1回延長可能
- プレイ時間は `game_limit.txt` に保存、日付が変わるとリセット

### フェーズファイル（current_phase.txt）

launcher.ahk との通信手段。

| 値 | 意味 |
|---|---|
| `lock` | サボり監視ON |
| `intermission` | サボり監視ON |
| `break` / `done` / `lunch` / `exercise` | サボり監視OFF |
| `croquis_done` | クロッキー正常終了（launcherが読んだ後に削除） |

### Discord通知タイミング（lock_window側）

- ロック中に終了 → 常に通知（クロッキー中 `isCroquis=true` の場合は送らない）
- 休憩中・中休み中に終了 → **作業ノルマ（`totalWorkGoalMinutes`）未達成なら通知、達成済みなら通知しない**（クロッキー中は対象外）
- リロード時は通知しない
- 作業達成・ゲーム制限到達時は通知しない（TrayTipのみ）

---

## 設定値一覧

### launcher.ahk

| 変数 | デフォルト | 説明 |
|---|---|---|
| `delayMinutes` | 30 | スリープ復帰からクロッキー開始までの待機分数 |
| `minSleepHours` | 6 | 就寝判定のスリープ時間閾値 |
| `skipWDays` | [7] | 自動起動しない曜日（7=土） |
| `mealPauseStartH/M` | 19:00 | 食事休憩開始（クロッキー延期判定用） |
| `mealPauseEndH/M` | 20:30 | 食事休憩終了（クロッキー延期判定用） |
| `croquisMode` | 0（ランダム） | クロッキーモード（1/2/3/4/0=ランダム） |
| `croquisRandomExcludeModes` | `[2]` | ランダムモードで除外するモード番号の配列 |
| `croquisModeParams` の `folder` | `.\croquis_models_1`〜`_3` | モードごとのモデル画像フォルダ（モード番号ごとに個別） |
| `croquisDelayMinutes` | 5 | ※現在未使用（delayMinutesが代替） |
| `croquisUsedLog` | `.\croquis_used.txt` | 使用済み画像リスト（全モード共通の1ファイルで一元管理） |
| `croquisShotDir` | `.\croquis_shots` | キャプチャ保存先（lock_window.ahkと合わせること） |
| `croquisReportWebhook` | （URL） | 週次レポート送信先Webhook |
| `croquisReportWDay` | 7 | 週次レポート送信曜日（7=土） |
| `croquisReportHour` | 15 | 週次レポート送信時刻 |
| `absentThresholdMin` | 15 | サボり判定の無操作時間（分） |
| `nightBlockDelay` | 30 | 就寝モード開始からブロックまでの分数 |
| `discordWebhook` | （URL） | サボり復帰・launcher終了通知先 |
| `saboWarnWebhook` | （URL） | サボり警告専用Webhook（別サーバー） |
| `overtimeWorkWindowTitles/Processes` | `["CLIPStudioPaint.exe"]` | 残業計測の対象ウィンドウ（lock_window.ahkの`workWindowTitles/Processes`と合わせること） |
| `overtimeIdleToleranceSecs` | 60 | 残業計測時の無操作許容時間（秒・lock_window.ahkの`workIdleToleranceSecs`と合わせること） |

### lock_window.ahk（主要なもの）

| 変数 | デフォルト | 説明 |
|---|---|---|
| `autoLockSecs` | 1500 | 自動起動時のロック時間（秒） |
| `autoBreakSecs` | 300 | 自動起動時の休憩時間（秒） |
| `autoTotalSets` | 3 | 自動起動時のセット数 |
| `croquisLockSecs` | 1500 | ※現在は引数で受け取るため実質未使用 |
| `croquisShotDir` | `.\croquis_shots` | キャプチャ保存先 |
| `croquisCaptureWait` | 180 | タイマー終了から撮影までの猶予（秒） |
| `croquisBreakSecs` | 600 | 全セット終了後の休憩時間（秒） |
| `mealPauseStartH/M` | 19:00 | 食事休憩開始 |
| `mealPauseEndH/M` | 20:30 | 食事休憩終了 |
| `exerciseUnlockMinutes` | 60 | 運動モード時間（分） |
| `lunchBreakMinutes` | 50 | 昼休み時間（分） |
| `intermissionMinutes` | 15 | 中休み時間（分） |
| `intermissionAddSets` | 2 | 中休みタイムアウト時の追加セット数 |
| `totalWorkGoalMinutes` | 150 | 作業達成に必要な時間（分・通常時） |
| `workGoalMinMinutes` | 90 | 超過繰り越しによるノルマ短縮の下限（分） |
| `workIdleToleranceSecs` | 60 | 作業カウント時の無操作許容時間（秒） |
| `focusModeAutoFromSet` | 3 | 集中モード自動発動の開始セット |
| `focusModeAutoChance` | 30 | 集中モード自動発動確率（%） |
| `breakDeferGraceSecs` | 5 | 休憩延期の猶予時間（秒） |
| `gameLimitMinutes` | 120 | ゲーム制限時間（分） |
| `gameLimitExtendSets` | 1 | 制限延長に必要なセット数 |
| `nextdnsApiKey` | （キー） | NextDNS APIキー |
| `nextdnsProfile` | 5f2c95 | NextDNSプロファイルID |
| `discordWebhook` | （URL） | Discord通知先 |

---

## 既知の問題・注意点

### クロッキーと食事休憩の干渉（修正済み）

クロッキーのロックタイマー終了直前（残り約300ms以下）のタイミングで `CheckMealPause`（15秒ごと）が発火すると、`g.isPaused = true` がセットされ `CroquisLockTick` が `remaining <= 0` の分岐に到達できなくなる問題があった。**修正済み：** `CheckMealPause` の冒頭で `isCroquis` なら即 return するようにした。これによりクロッキー中は食事休憩による中断を一切受けず、最後まで進行してから通常タイマーに戻る（launcher 側が食事時間帯の開始判定を行っているため、食事時間帯に食い込むのは稀なケース）。

### CaptureCountdownTick のトップレベル化

AHKv2 の多重ネスト関数ではクロージャが正しく動作しない場合があるため、`CaptureCountdownTick` をトップレベル関数として定義し、必要な値（`captureEnd` 等）を `croquisCaptureEnd` 等のグローバル変数で受け渡している。

### ゲームプレイ制限の「1日1回延長」機能（未実装・対応保留）

`gameLimitExtendSets` や `g.gameExtendSetsLeft` / `g.gameExtendUsed` / `gameExtendSetsDone` は変数として用意されているだけで、実際に延長を判定・付与する処理が存在しない。`g.gameLimitReached` を `false` に戻す経路がどこにもないため、**一度制限に達するとそのセッション中は解除されない。** 実装するなら、中休み（intermission）やセット完了のタイミングで完了セット数をカウントし、`gameLimitExtendSets` に達したら `g.gameLimitReached := false` にリセットする処理を追加する必要がある。

### settings_editor.html

- ブラウザで開いてAHKファイルをドロップ → 値を編集 → 「保存」でダウンロード → 元ファイルに上書き
- ファイル内の変数を正規表現でパースするため、変数名が他の文字列中に含まれるケースでは誤マッチする可能性がある（現状の設定変数では問題なし）
- 対応する値の型は `int`（整数）・`select`（選択式）・`str`（文字列）・**`arr`（数値の配列）**
  - `arr` 型は `croquisRandomExcludeModes` のような `[3, 4]` 形式のAHK配列を、画面上は `3, 4` というカンマ区切りの文字列として編集できるようにする。保存時に `[3, 4]` の形へ自動変換して書き込む（空欄なら `[]`）

---

## ファイル・フォルダ構成

```
スクリプトフォルダ/
├── launcher.ahk
├── lock_window.ahk
├── settings_editor.html
├── croquis_models_1/       ← モデル画像フォルダ（モード1用・手動作成）
├── croquis_models_2/       ← モデル画像フォルダ（モード2用・手動作成）
├── croquis_models_3/       ← モデル画像フォルダ（モード3用・手動作成）
├── croquis_shots/         ← キャプチャ保存先（自動作成）
├── sabo_logs/             ← サボりログ（自動作成）
├── croquis_used.txt        ← 使用済み画像リスト（全モード共通・自動作成）
├── current_phase.txt      ← フェーズ共有ファイル（自動管理）
├── last_sleep.txt         ← 最後のスリープ時刻（自動管理）
├── day_state.txt          ← 睡眠ベースの日付境界dayId（自動管理・書き込みはlauncher.ahkのみ）
├── launcher_running.txt   ← 異常終了検知用sentinel（自動管理）
├── night_block_flag.txt   ← 就寝ブロック実行フラグ（自動管理）
├── exercise_log.txt       ← 運動ボタン使用日記録（自動管理・カレンダー日付基準）
├── lunch_log.txt          ← 昼休みボタン使用日記録（自動管理・カレンダー日付基準）
├── game_limit.txt         ← ゲームプレイ時間記録（自動管理・カレンダー日付基準）
├── work_goal.txt          ← 作業ノルマ進捗記録（自動管理・dayId基準）
├── overtime_work.txt      ← 残業時間記録（自動管理・dayId基準・書き込みはlauncher.ahkのみ）
└── croquis_report_log.txt ← 週次レポート送信結果ログ（自動管理・上書き）
```
